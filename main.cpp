#include "controller.hpp"

int main()
{
    Controller* objController = new Controller();

    // // Prueba lista
    // LinkedList<User*>* list = new LinkedList<User*>();
    // User* u1 = new User("a", "a", "a");
    // list->addFinal(u1);
    // User* u2 = new User("b", "b", "b");
    // list->addFinal(u2);
    // User* u3 = new User("c", "c", "c");
    // list->addFinal(u3);

    // list->printList();
    return 0;
}